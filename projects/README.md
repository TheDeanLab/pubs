# Projects (git submodules)

Each writing project (grant, manuscript, response-to-reviewers, etc.) lives in its own private repository
and is included in the hub as a **git submodule** under `projects/`.

If you do not have access to a project repo, you will not be able to fetch that submodule.
